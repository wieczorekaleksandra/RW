# Przyklad 4.11 / Sc1 z wykladu RW_04 — niespojnosc przez invokes
#
# Z wykladu (AL):
#   A causes f;
#   B causes ~f;
#   C causes g;
#   A invokes C after 1.       (w naszym DS1: a triggers c after 1)
#   Obs = puste
#   Acs1 = {(A, 1), (B, 3)}
#
# Slad czasowy:
#   t=1: A startuje
#   t=2: A konczy, A invokes C → C startuje w t=2+1=3
#   t=3: B startuje (z ACS), C startuje (z triggera) → KONFLIKT
#
# W DS1: walidator nie wykrywa (C nie ma w ACS, dodaje go solver),
#        ale solver odrzuca przez sprawdzenie sekwencyjnosci → 0 modeli.

DOMAIN:
a duration 1
b duration 1
c duration 1
a causes f after 1
b causes ~f after 1
c causes g after 1
a triggers c after 1

SCENARIO:
OBS:
ACS:
(a, 1)
(b, 3)

QUERIES:
possibly Sc
